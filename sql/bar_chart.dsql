select to_char(authored, 'YYYY-MM') "Date", corpus "Corpus", count(doc_id) "Documents"
    from {table_name} {where_clause}
    group by to_char(authored, 'YYYY-MM'), corpus 
    order by to_char(authored, 'YYYY-MM'), corpus;
